export enum UserRole {
  ADMIN = 'admin',
  TEACHER = 'teacher',
  STUDENT = 'student',
  GUEST = 'guest'
}

export enum Company{
  WEBSTAR = 'webstar',
}

export enum CategoryRole{
  EXAM = 'exam',
  TASK = 'task'
}